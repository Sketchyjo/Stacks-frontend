import { View, Text } from 'react-native'
import React from 'react'
import { Home, Heart, Star } from 'lucide-react-native'

const index = () => {
  return (
    <View className="flex-1 justify-center items-center">
    </View>
  )
}

export default index